import math

cat1= int(input("ingrese el valor del cateto 1"))
cat2= int(input("ingrese el valor del cateto 2"))

hipot= float(math.sqrt (pow(cat1,2) + pow(cat2,2) ))

print("El resultado es:", hipot)